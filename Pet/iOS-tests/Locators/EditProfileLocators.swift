import XCTest

enum EditProfileLocators {
    static let nameField = "nameField"
    static let saveButton = "saveButton"
    static let cancelButton = "cancelButton"
    static let errorText = "validationError"
}
